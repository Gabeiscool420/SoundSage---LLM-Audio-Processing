PreProcess

This is where files will be moved to by the custom LLM written file_management.py before processing.
once the specified files are all detected as present within this folder a signal will be sent to 
the AutoGain (or other future AudioTool) interaction Script.
